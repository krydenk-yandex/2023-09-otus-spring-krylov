spring:
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    open-in-view: false
    generate-ddl: false
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always
  liquibase:
    enabled: true